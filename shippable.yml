language: python
reset_minion: true

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  - pypy
  
install:
  - pip install -r requirements.txt

branches:
  only:
    - master
    - env_fault 

script:
  - nosetests compmake 
  
notifications:
  email:
   - censi@mit.edu